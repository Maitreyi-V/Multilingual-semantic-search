def search_sentence_or_word(text: str, query: str) -> list:
    """

    Args:
        text (str)- The entire text content in which to search.
        query (str)- The word or sentence to search for.

    Returns:
        list- A dummy list of results matching the query.

    Example:
        >>> search_sentence_or_word("hello world\nhi there", "hello")
        ["hello world"]
    """
    
    return ["This is a dummy matching line.", "Another example result."]
